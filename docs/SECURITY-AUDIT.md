# Brew-POS Security Audit

Scope: data-leak and network-attack surface across the BrewPOS stack at
`/home/lenovo/Hermes-Project/Brew-POS/`.

Method: step-by-step static + light dynamic review. Each step is written up
below in order, with findings tagged `[CRITICAL]`, `[HIGH]`, `[MEDIUM]`,
`[LOW]`, `[INFO]`. No live exploitation — findings come from code reading,
config inspection, and (where safe) HEAD/GET probes.

---

## Step 1 — Reconnaissance: config, run scripts, services, exposed ports

(to be filled in)
